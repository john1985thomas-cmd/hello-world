# Hello World

A minimal Hello World web app built with Python — no external dependencies.

## Run

```bash
python3 app.py
```

Then open [http://localhost:8000](http://localhost:8000) in your browser.
